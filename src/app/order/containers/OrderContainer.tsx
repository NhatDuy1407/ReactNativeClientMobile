import React from 'react';
import { View } from 'react-native';
import ProductListContainer from '../product/containers/ProductListContainer';

const OrderContainer = () => {
  return (
    <View>
      <ProductListContainer />
    </View>
  );
};

export default OrderContainer;
